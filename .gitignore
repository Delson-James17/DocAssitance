node_modules/
dist/
.env
*.log

# Packaged desktop builds (electron-builder output)
release/
release-build-temp/

# whisper.cpp binaries + model (~150MB) — run `npm run whisper:setup`
vendor/

# Local Netlify folder
.netlify
